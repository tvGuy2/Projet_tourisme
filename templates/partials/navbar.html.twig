<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.2/font/bootstrap-icons.css">
<nav class="navbar navbar-expand-lg navbar-light p-3 " style="background-color: #99908E; ">
    <!-- Barre de navigation -->
    <div class="container-fluid">
        <a class="navbar-brand " style="" href="#"><i class="bi bi-airplane"></i> FC-tourisme</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mb-2 mb-lg-0 me-auto">
                <li class="nav-item">
                    <a class="nav-link hover-underlign "href="#"><i class="bi bi-house-door-fill"></i> Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_etablissements') }}"><i class="bi bi-buildings"></i> Etablissements</a>
                </li>
                {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_etablissements_favoris') }}"><i class="bi bi-buildings"></i> Favoris</a>
                </li>
                {% endif %}
            </ul>

            <ul class="navbar-nav mb-2 mb-lg-0">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    {% if app.user.pseudo %}
                        <span class="me-4 navbar-text text-white">{{ app.user.pseudo}}</span>
                        {% else %}
                    <span class="me-4 navbar-text text-white">{{ app.user.email }}</span>
                            {% endif %}
                    <li>
                        <a class="btn btn-danger" href="{{ path('app_logout') }}" role="button"> Deconnexion</a>
                    </li>

                    {% else %}
                        <li >
                            <a class="btn btn-info me-2" href="{{ path('app_register') }}" role="button"> Inscription</a>
                        </li>
                        <li >
                            <a class="btn btn-success" href="{{ path('app_security') }}" role="button"> Connexion</a>
                        </li>
                {% endif %}

            </ul>

        </div>
    </div>
</nav>


