{% extends 'base.html.twig' %}

{% block title %}Hello EtablissementController!{% endblock %}

{% block body %}


    <div class="container d-flex justify-content-center mt-5 mb-5">
            <div class="card text-center" style="width: 60rem;">
                {% if etablissement.image is null %}
                    <img src="https://www.genifoncier.com/assets/images/no_image.jpg" class="card-img-top" alt="..." >
                {% else %}
                    <img src="{{ etablissement.image }}" class="card-img-top" alt="...">
                {% endif %}
                <br class="card-body">
                    <h5 class="card-title">{{ etablissement.nom}}</h5></br>
                <p class="card-text">Cette établissement à été créé le <strong>{{ etablissement.createdAt | date("d/m/Y")  }}.</strong><br>  Il se situe dans la ville de {{ etablissement.ville.nom}} dans le département du {{ etablissement.ville.numDepartement}} alias {{ etablissement.ville.departement}}.<br>Cet établissement est considéré comme un {% for categorie in etablissement.categorie %}
                        <strong>{{ categorie.nom }}</strong>
                        {% endfor %}.</p>
                    <p class="card-text">Adresse :  <strong>{{ etablissement.adresse }} {{ etablissement.ville.codePostal}} </strong> </p>
                    <p class="card-text">{{ etablissement.description}}</p>
                <p class="card-text">Pour prendre une réservation veuillez nous contacter par téléphone <strong>{{ etablissement.telephone }}</strong></br> ou bien par mail <strong>{{ etablissement.email}}</strong></p>
                </div>
            </div>
    </div>




{% endblock %}
